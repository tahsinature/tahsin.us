import React, { Suspense } from "react";
import { Skeleton } from "@/components/ui/skeleton";

const Page = async () => {
  return (
    <div>
      <h1>TestPage</h1>
      <hr />
    </div>
  );
};

export default Page;
