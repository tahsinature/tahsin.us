info:
  name: ops
  type: http
  seq: 3

http:
  method: POST
  url: "{{backend_host}}/api/ops/cache/bust"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
