info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: "{{backend_host}}/api/ops/health"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
