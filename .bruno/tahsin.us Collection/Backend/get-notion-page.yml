info:
  name: get-notion-page
  type: http
  seq: 8

http:
  method: GET
  url: "{{backend_host}}/api/notion/:pageId"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit
  params:
    path:
      - name: pageId
        value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
