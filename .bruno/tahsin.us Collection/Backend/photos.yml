info:
  name: photos
  type: http
  seq: 6

http:
  method: GET
  url: "{{backend_host}}/api/photos"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit
  params:
    query:
      - name: trip-id
        value: ""
        enabled: false

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
