info:
  name: get-named-page
  type: http
  seq: 6

http:
  method: GET
  url: "{{backend_host}}/api/pages/:name"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
