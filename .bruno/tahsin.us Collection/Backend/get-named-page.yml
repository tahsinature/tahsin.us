info:
  name: get-named-page
  type: http
  seq: 7

http:
  method: GET
  url: "{{backend_host}}/api/pages/:name"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit
  params:
    path:
      - name: name
        value: about

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
