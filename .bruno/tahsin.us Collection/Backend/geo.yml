info:
  name: geo
  type: http
  seq: 2

http:
  method: GET
  url: "{{backend_host}}/api/geo"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
