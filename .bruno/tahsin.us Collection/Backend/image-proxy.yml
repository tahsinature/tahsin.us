info:
  name: image-proxy
  type: http
  seq: 6

http:
  method: GET
  url: "{{backend_host}}/api/image-proxy?url=https://example.com/image.jpg"
  headers: []
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
