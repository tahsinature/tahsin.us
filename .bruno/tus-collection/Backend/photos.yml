info:
  name: photos
  type: http
  seq: 5

http:
  method: GET
  url: "{{backend_host}}/api/photos?favonly=true"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: favonly
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
