info:
  name: resolve-files
  type: http
  seq: 8

http:
  method: POST
  url: "{{backend_host}}/api/notion/resolve-files"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
