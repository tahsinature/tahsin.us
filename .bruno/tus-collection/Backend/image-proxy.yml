info:
  name: image-proxy
  type: http
  seq: 2

http:
  method: GET
  url: "{{backend_host}}/api/image-proxy?url=https://example.com/image.jpg"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
