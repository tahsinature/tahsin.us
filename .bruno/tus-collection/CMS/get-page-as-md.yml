info:
  name: get-page-as-md
  type: http
  seq: 1

http:
  method: GET
  url: https://api.notion.com/v1/pages/:page_id/markdown
  params:
    - name: page_id
      value: "{{process.env.TRAVEL_PID}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: https://developers.notion.com/reference/retrieve-page-markdown
