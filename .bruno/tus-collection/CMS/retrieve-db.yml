info:
  name: retrieve-db
  type: http
  seq: 2

http:
  method: GET
  url: https://api.notion.com/v1/databases/:database_id
  params:
    - name: database_id
      value: "{{process.env.PHOTOS_DS_ID}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: https://developers.notion.com/reference/retrieve-page-markdown
